# 部署指南 - Render 免费部署

本指南将帮助您将项目部署到 Render 平台（免费）。

## 前提条件

1. GitHub 账号
2. Render 账号（使用 GitHub 登录）

## 步骤1：上传代码到 GitHub

### 1.1 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - Repository name: `dou-image-tool`（或任意名称）
   - Description: `批量抠图和文案生成工具`
   - 选择 **Public**（公开）或 **Private**（私有）
   - **不要**勾选 "Add a README file"
   - **不要**添加 .gitignore（我们已经有了）
3. 点击 "Create repository"

### 1.2 推送代码到 GitHub

在项目目录执行以下命令：

```bash
cd /Users/haihui/dou

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 创建第一次提交
git commit -m "Initial commit: 批量抠图和文案生成工具"

# 添加远程仓库（替换成你的 GitHub 用户名和仓库名）
git remote add origin https://github.com/你的用户名/dou-image-tool.git

# 推送代码
git branch -M main
git push -u origin main
```

## 步骤2：在 Render 部署

### 2.1 创建 Render 账号

1. 访问 https://render.com/
2. 点击 "Get Started for Free"
3. 使用 GitHub 账号登录

### 2.2 创建 Web Service

1. 登录后，点击 "New +"
2. 选择 "Web Service"
3. 连接你的 GitHub 仓库：
   - 如果是第一次，点击 "Connect account" 授权 Render 访问 GitHub
   - 找到并选择 `dou-image-tool` 仓库
4. 填写服务配置：
   - **Name**: `dou-image-tool`（服务名称）
   - **Region**: 选择最近的地区（推荐 Singapore）
   - **Branch**: `main`
   - **Root Directory**: 留空
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn web_app:app`
   - **Instance Type**: **Free**（免费套餐）

5. 点击 "Create Web Service"

### 2.3 等待部署完成

- Render 会自动：
  1. 克隆你的代码
  2. 安装依赖（这可能需要 5-10 分钟）
  3. 启动应用

- 部署状态：
  - 🟡 Building: 正在构建
  - 🟢 Live: 部署成功
  - 🔴 Failed: 部署失败

### 2.4 访问你的应用

部署成功后，Render 会提供一个公开 URL，格式类似：
```
https://dou-image-tool.onrender.com
```

将这个链接分享给别人，他们就能访问你的应用了！

## 步骤3：更新代码

以后如果你修改了代码，只需要：

```bash
cd /Users/haihui/dou
git add .
git commit -m "更新描述"
git push
```

Render 会自动检测到更新并重新部署。

## 重要说明

### 免费套餐限制

- **自动休眠**：15 分钟无访问后会自动休眠
- **唤醒时间**：首次访问需要等待 30-60 秒
- **磁盘空间**：上传的文件会在应用重启后丢失
- **每月限制**：750 小时免费运行时间

### 持久化存储

如果需要保存用户上传的文件，建议：
1. 使用云存储（如 Cloudinary、AWS S3）
2. 升级到付费套餐

## 常见问题

### Q: 部署失败怎么办？

A: 检查 Render 的部署日志：
1. 进入你的 Web Service
2. 点击 "Logs"
3. 查看错误信息

### Q: 应用很慢？

A: 免费套餐会在 15 分钟无访问后休眠。首次访问需要等待唤醒。

### Q: 如何查看访问量？

A: Render Dashboard 提供基本的访问统计。

### Q: 可以绑定自定义域名吗？

A: 可以！在 Render Dashboard 的 Settings 中添加自定义域名。

## 技术支持

- Render 文档: https://render.com/docs
- 项目问题: 在 GitHub Issues 中提问

## 备选方案

如果 Render 不适合，可以考虑：
- **Railway**: https://railway.app/（也提供免费套餐）
- **Fly.io**: https://fly.io/（免费套餐，需要信用卡）
- **PythonAnywhere**: https://www.pythonanywhere.com/（免费套餐有限制）

---

祝部署顺利！🎉
