# Web 抠图工具使用指南

## 🎉 功能介绍

一个简单易用的 Web 界面，用于批量去除图片背景。

**特点：**
- ✅ 拖拽上传，支持多张图片
- ✅ 自动去除背景，保持高清
- ✅ 单独下载或打包下载全部
- ✅ 无需命令行，完全可视化
- ✅ 本地运行，数据安全

---

## 🚀 启动服务

### 方法1：命令行启动

```bash
cd /Users/haihui/dou
source venv/bin/activate
python web_app.py
```

### 方法2：双击运行（可选）

创建一个启动脚本：

```bash
# 创建启动脚本
cat > /Users/haihui/dou/start_web.sh << 'EOF'
#!/bin/bash
cd /Users/haihui/dou
source venv/bin/activate
python web_app.py
EOF

# 添加执行权限
chmod +x /Users/haihui/dou/start_web.sh
```

以后双击 `start_web.sh` 即可启动。

---

## 📱 使用步骤

### 1. 启动服务

运行 `python web_app.py` 后，你会看到：

```
============================================================
                    Web抠图工具
============================================================

访问地址: http://localhost:5000

功能:
  1. 上传图片（支持多张）
  2. 自动去除背景
  3. 下载处理后的图片

按 Ctrl+C 停止服务
============================================================
```

### 2. 打开浏览器

在浏览器中访问：**http://localhost:5000**

### 3. 上传图片

**方式1：点击上传**
- 点击上传区域
- 选择一张或多张图片
- 支持的格式：JPG、PNG、WEBP、BMP

**方式2：拖拽上传**
- 直接把图片拖到上传区域
- 支持一次拖拽多张

### 4. 开始处理

- 点击"开始处理"按钮
- 等待处理完成（会显示进度）

### 5. 下载结果

**单独下载：**
- 每张图片旁边有"下载"按钮
- 点击即可下载单张

**批量下载：**
- 点击"打包下载全部"
- 会自动打包成 ZIP 文件
- 下载到你的电脑

---

## 🎨 界面说明

### 主要区域

1. **上传区域** - 拖拽或点击上传图片
2. **预览区域** - 显示上传的图片缩略图
3. **统计信息** - 显示处理成功/失败的数量
4. **结果列表** - 每张图片的处理结果和下载按钮

### 按钮功能

- **开始处理** - 批量去除背景
- **清空** - 清除所有图片和结果
- **打包下载全部** - 下载所有处理后的图片（ZIP格式）
- **下载** - 下载单张处理后的图片

---

## 💡 使用技巧

### 批量处理

一次可以上传多张图片，程序会自动批量处理。

### 文件命名

处理后的文件名会自动添加 `_nobg.png` 后缀：
- 原文件：`product.jpg`
- 处理后：`product_nobg.png`

### 清空结果

如果要处理新的一批图片：
1. 点击"清空"按钮
2. 重新上传新图片

---

## 🔧 高级配置

### 修改端口

默认端口是 5000，如果需要修改，编辑 `web_app.py` 最后一行：

```python
app.run(debug=True, host='0.0.0.0', port=5000)  # 修改端口号
```

### 修改抠图参数

编辑 `config.py` 中的 `REMBG_CONFIG` 配置：

```python
REMBG_CONFIG = {
    'model': 'u2net',          # 模型选择
    'alpha_matting': False,    # 是否使用 alpha matting
    # ...
}
```

### 限制文件大小

默认最大 50MB，修改 `web_app.py`：

```python
app.config['MAX_CONTENT_LENGTH'] = 50 * 1024 * 1024  # 修改大小限制
```

---

## ❓ 常见问题

**Q: 浏览器无法访问？**

A: 检查：
1. 服务是否启动成功
2. 地址是否正确：http://localhost:5000
3. 防火墙是否阻止

**Q: 上传失败？**

A: 检查：
1. 文件格式是否支持（JPG/PNG/WEBP/BMP）
2. 文件大小是否超过限制（默认50MB）
3. 网络连接是否正常

**Q: 处理速度慢？**

A: 首次运行会下载 AI 模型（约180MB），之后会很快。处理速度取决于：
- 图片数量和尺寸
- 电脑性能

**Q: 如何停止服务？**

A: 在终端按 `Ctrl+C` 即可停止。

---

## 🌐 在局域网使用

如果想让同一WiFi下的其他设备访问：

1. 查看你的电脑IP地址：
   ```bash
   ifconfig | grep "inet "
   ```

2. 在其他设备浏览器访问：
   ```
   http://你的IP:5000
   ```

例如：`http://192.168.1.100:5000`

---

## 📂 文件存储位置

- **上传临时文件**：`web_uploads/`（处理后自动删除）
- **处理后文件**：`web_outputs/`（可手动清空）

---

## 🎯 下一步

启动服务试试吧！

```bash
cd /Users/haihui/dou
source venv/bin/activate
python web_app.py
```

然后在浏览器打开：**http://localhost:5000**

享受可视化的抠图体验！ ✨
